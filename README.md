# Job Aggregator API

FastAPI приложение для агрегации вакансий с HeadHunter и интеграции с Telegram ботами. Предоставляет полный API для управления вакансиями, аутентификации пользователей и работы с резюме на HH.ru.

## Возможности

- 🔍 Поиск вакансий через API HeadHunter
- 📊 Полный CRUD API для управления вакансиями
- 🔧 Фильтрация и сортировка вакансий
- 👤 Аутентификация пользователей (JWT токены + пароль)
- 🤖 Интеграция с Telegram ботами (регистрация через Telegram ID)
- 📄 Полное управление резюме на HH.ru (создание, публикация, обновление)
- 🔗 OAuth 2.0 авторизация через HH.ru
- 🎯 Поиск вакансий по активному резюме
- 🗄️ PostgreSQL база данных с автоматическими миграциями
- 🐳 Docker контейнеризация (development + test environments)
- 📈 Автоматическое обновление данных через планировщик
- 🧪 Полное тестовое покрытие с CI/CD
- 🔄 Миграции базы данных (Alembic)
- 🚀 CI/CD (GitHub Actions, GitLab CI)
- 📝 Pre-commit хуки для автоматического форматирования кода

## Технологии

- **Backend**: FastAPI 0.116.1, Python 3.11
- **База данных**: PostgreSQL 15
- **ORM**: SQLAlchemy 2.0.43
- **Аутентификация**: JWT (python-jose), Argon2
- **Контейнеризация**: Docker, Docker Compose
- **Тестирование**: pytest 8.4.2, coverage 7.10.7
- **Планировщик**: APScheduler 3.11.0
- **Миграции**: Alembic 1.17.2
- **Форматирование**: Black 25.9.0, isort 7.0.0
- **Линтинг**: Ruff 0.1.5, flake8 7.3.0
- **CI/CD**: GitHub Actions, GitLab CI
- **HTTP клиент**: httpx 0.28.1

## Быстрый старт

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd job_aggregator
```

### 2. Настройка окружения (локальный запуск)

Если вы запускаете через Docker, этот шаг не нужен — контейнер сам установит зависимости из `requirements.txt` согласно `Dockerfile`.

```bash
# Создание виртуального окружения
python -m venv venv

# Активация (Windows)
.\venv\Scripts\Activate.ps1

# Активация (Linux/Mac)
source venv/bin/activate

# Установка зависимостей (только для локального запуска без Docker)
pip install -r requirements.txt
```

### 3. Запуск с Docker

При запуске в Docker не требуется локально ставить зависимости и настраивать venv — всё выполнится внутри контейнера.

```bash
# Запуск всех сервисов
docker-compose up -d

# Проверка статуса
docker-compose ps

# Остановка всех сервисов без удаления контейнеров
docker-compose stop

# Остановка всех сервисов с удалением контейнеров и volumes
docker-compose down -v
```

### 4. Запуск локально

```bash
# Создание виртуального окружения (если не создано)
python -m venv venv

# Активация (Windows)
.\venv\Scripts\Activate.ps1

# Активация (Linux/Mac)
source venv/bin/activate

# Установка зависимостей
pip install -r requirements.txt

# Запуск основной базы данных
docker-compose up -d db

# Применение миграций
alembic upgrade head

# Запуск приложения
uvicorn app.main:app --reload
```

## API Документация

После запуска приложения документация доступна по адресам:

- **Swagger UI**: http://localhost:8000/api/v1/docs
- **ReDoc**: http://localhost:8000/api/v1/redoc
- **OpenAPI**: http://localhost:8000/openapi.json

## Настройка сбора вакансий

### Общая информация

Система автоматически собирает вакансии с HH.ru по расписанию. Все настройки можно изменить через конфигурационные файлы.

### Конфигурация расписания

**Файл**: `app/scheduler.py`

```python
scheduler.add_job(
    job_fetch_vacancies,
    "interval",
    minutes=60,            # Интервал в минутах
    next_run_time=datetime.now(),
    misfire_grace_time=30, # Допустимое время задержки
)
```

**Доступные интервалы**:

* `seconds` — секунды
* `minutes` — минуты
* `hours` — часы
* `days` — дни

### Настройка параметров поиска

**Файл**: `app/services/hh_api.py`

```python
async def fetch_vacancies(
    keyword: str = "Python",  # Ключевое слово поиска
    area: int = 1002,         # Код региона
    per_page: int = 10,       # Количество вакансий (max 100)
    page: int = 0,            # Номер страницы
):
```

**Популярные коды регионов**:

* `1` — Москва
* `2` — Санкт-Петербург
* `1002` — Санкт-Петербург (альтернативный)
* Полный список регионов: [https://api.hh.ru/areas](https://api.hh.ru/areas)

### Примеры конфигураций

**Частый сбор (каждые 30 минут)**:

```python
minutes = 30
```

**Сбор для другого языка/технологии**:

```python
items = fetch_vacancies("JavaScript", area=1, per_page=20)
```

**Увеличение количества вакансий**:

```python
per_page = 50  # до 100 вакансий за запрос
```

### Примечания

* Расписание автоматически запускается при старте приложения
* При ошибках сбора подробности логируются в лог-файл
* Рекомендуемый интервал — не менее 10 минут для соблюдения лимитов API HH.ru
* При изменении параметров требуется перезапуск приложения

### API Endpoints

#### Аутентификация
- `POST /auth/register` - Регистрация пользователя
- `POST /auth/register/telegram` - Регистрация через Telegram
- `POST /auth/login` - Вход в систему
- `POST /auth/refresh` - Обновление токена доступа

#### Пользователи
- `GET /users/current` - Получить текущего пользователя
- `GET /users/{user_id}` - Получить пользователя по ID
- `DELETE /users/{user_id}` - Удалить пользователя
- `GET /users/telegram/{telegram_id}` - Получить Telegram пользователя
- `POST /users/telegram/` - Создать Telegram пользователя

#### Вакансии
- `GET /vacancies` - Получить список вакансий с фильтрацией
  - Параметры: `company`, `location`, `skip`, `limit`, `sort_by`
- `POST /vacancies` - Создать новую вакансию
- `PUT /vacancies/{id}` - Обновить вакансию
- `DELETE /vacancies/{id}` - Удалить вакансию

#### HH.ru Интеграция
- `GET /api/v1/auth/hh/login` - Начать OAuth авторизацию HH.ru
- `GET /api/v1/auth/hh/login_url` - Получить URL для авторизации HH.ru
- `GET /api/v1/auth/hh/callback` - Обработчик OAuth callback
- `GET /api/v1/auth/hh/token/{user_id}` - Проверить статус токена HH.ru
- `GET /api/v1/auth/hh/me` - Получить информацию о текущем пользователе HH.ru
- `GET /api/v1/auth/hh/resumes` - Получить список резюме
- `POST /api/v1/auth/hh/resumes/test_create` - Создать тестовое резюме
- `POST /api/v1/auth/hh/resumes/create_resume` - Создать резюме
- `GET /api/v1/auth/hh/resumes/active` - Получить активное резюме
- `GET /api/v1/auth/hh/resumes/active/vacancies` - Поиск вакансий по активному резюме
- `POST /api/v1/auth/hh/resumes/select/{resume_id}` - Выбрать активное резюме
- `POST /api/v1/auth/hh/resumes/{resume_id}/publish` - Опубликовать резюме
- `GET /api/v1/auth/hh/resumes/{resume_id}/vacancies` - Поиск вакансий по резюме
- `PUT /api/v1/auth/hh/resumes/{resume_id}` - Обновить резюме
- `DELETE /api/v1/auth/hh/resumes/{resume_id}` - Удалить резюме

#### Системные
- `GET /health` - Проверка состояния приложения
- `GET /health/db` - Проверка состояния базы данных

## Тестирование

### Быстрый старт

```bash
# Полный цикл тестирования (БД → тесты → остановка)
python scripts/test_runner.py full

# Тесты с покрытием кода (требует запущенную БД)
python scripts/test_runner.py coverage

# Только тесты (БД должна быть запущена)
python scripts/test_runner.py tests
```

### Структура тестов

- **test_auth.py** - Тесты аутентификации и авторизации
- **test_vacancies.py** - Тесты CRUD операций с вакансиями
- **test_settings.py** - Тесты конфигурации приложения
- **conftest.py** - Фикстуры и настройка pytest

### Запуск через Docker

```bash
# Запуск тестов в Docker контейнере
docker-compose -f docker-compose.test.yml run --rm test_api pytest
```

📖 **Подробная информация о тестировании**: [tests/README.md](tests/README.md)

## CI/CD

Проект использует автоматическую проверку кода и тестирование:

### GitHub Actions

- **Alembic Check**: Проверка миграций базы данных
- **Testing**: Полный цикл тестирования с покрытием кода
- **Триггеры**: Push в `main`/`develop`, Pull Requests

### GitLab CI

- **Alembic Check**: Валидация миграций
- **Pytest**: Запуск тестов с отчетом о покрытии
- **Code Quality**: Проверка форматирования (Black, isort)
- **Linting**: Проверка кода (flake8)

### Pre-commit хуки

Автоматическая проверка кода перед коммитом:

```bash
# Установка pre-commit хуков
pip install pre-commit
pre-commit install

# Ручной запуск всех проверок
pre-commit run --all-files
```

## Структура проекта

```
job_aggregator/
├── app/                    # Основное приложение
│   ├── crud/              # CRUD операции (создание, чтение, обновление, удаление)
│   │   ├── auth.py        # Операции аутентификации
│   │   ├── hh_resume.py   # Управление резюме HH.ru
│   │   ├── hh_token.py    # Токены HH.ru
│   │   ├── user.py        # Пользователи
│   │   └── vacancy.py     # Вакансии
│   ├── models/            # SQLAlchemy модели
│   │   ├── hh_token.py    # Токены HH.ru
│   │   ├── my_api_token.py # API токены
│   │   ├── user.py        # Пользователи
│   │   └── vacancy.py     # Вакансии
│   ├── routes/            # API маршруты (REST endpoints)
│   │   ├── auth.py        # Аутентификация
│   │   ├── hh_auth.py     # OAuth HH.ru
│   │   ├── users.py       # Управление пользователями
│   │   └── vacancies.py   # Управление вакансиями
│   ├── schemas/           # Pydantic схемы (валидация данных)
│   │   ├── auth.py        # Схемы аутентификации
│   │   ├── hh_resume.py   # Схемы резюме
│   │   ├── user.py        # Схемы пользователей
│   │   └── vacancy.py     # Схемы вакансий
│   ├── services/          # Бизнес-логика
│   │   ├── auth.py        # Сервис аутентификации
│   │   ├── hh_api.py      # API HH.ru
│   │   ├── hh_auth.py     # OAuth HH.ru
│   │   └── vacancy_formatter.py # Форматирование вакансий
│   ├── examples/          # Примеры данных
│   │   ├── hh_resume.py   # Примеры резюме
│   │   └── vacancy.py     # Примеры вакансий
│   ├── config.py          # Конфигурация приложения
│   ├── database.py        # Настройка БД и сессий
│   ├── main.py            # Точка входа FastAPI
│   ├── scheduler.py       # Планировщик задач
│   ├── logger.py          # Логирование
│   ├── exceptions.py      # Обработка ошибок
│   └── dependencies.py    # Зависимости FastAPI
├── tests/                 # Тесты
│   ├── conftest.py        # Конфигурация pytest и фикстуры
│   ├── test_auth.py       # Тесты аутентификации
│   ├── test_settings.py   # Настройки для тестовой среды
│   ├── test_vacancies.py  # Тесты API вакансий
│   └── README.md          # Документация тестирования
├── scripts/               # Вспомогательные скрипты
│   ├── test_runner.py     # Автоматизированный запуск тестов
│   └── test_db.py         # Тестирование БД
├── migrations/           # Alembic миграции БД
│   ├── versions/          # Версии миграций
│   ├── env.py             # Окружение миграций
│   ├── script.py.mako     # Шаблон миграций
│   └── README             # Документация миграций
├── docker-compose.yml    # Основные сервисы
├── docker-compose.test.yml # Тестовая БД
├── .pre-commit-config.yaml # pre-commit хуки для автоматической проверки и форматирования кода
└── requirements.txt       # Зависимости Python
```

## Модели базы данных

### Основные модели

- **User**: Стандартные пользователи (username + email)
- **TelegramUser**: Пользователи Telegram ботов (наследуется от User)
- **UserAuth**: Аутентификационные данные пользователей (хэши паролей)
- **Vacancy**: Вакансии с HH.ru и других источников
- **HHToken**: OAuth токены для доступа к HH.ru API
- **RefreshToken**: JWT refresh токены для API аутентификации

### Связи между моделями

- User ↔ HHToken (1:N) - Пользователь может иметь несколько токенов HH.ru
- User ↔ RefreshToken (1:N) - Пользователь может иметь несколько refresh токенов
- User ↔ UserAuth (1:1) - Пользователь имеет аутентификационные данные
- TelegramUser ↔ User (1:1) - Telegram пользователь наследует от User
- Vacancy - независимая модель для хранения вакансий

## Конфигурация

### Переменные окружения

#### База данных
- `POSTGRES_USER` - пользователь PostgreSQL
- `POSTGRES_PASSWORD` - пароль PostgreSQL
- `POSTGRES_DB` - имя базы данных
- `POSTGRES_HOST` - хост базы данных
- `POSTGRES_PORT` - порт базы данных (по умолчанию: 5432)

#### Аутентификация
- `SECRET_KEY` - секретный ключ для JWT токенов
- `ALGORITHM` - алгоритм шифрования JWT (по умолчанию: HS256)
- `ACCESS_TOKEN_EXPIRE_MINUTES` - время жизни access токена в минутах

#### HH.ru API
- `HH_CLIENT_ID` - Client ID приложения HH.ru
- `HH_CLIENT_SECRET` - Client Secret приложения HH.ru
- `HH_REDIRECT_URI` - Redirect URI для OAuth

#### Настройки приложения
- `DISABLE_SCHEDULER` - отключить планировщик задач (1 для отключения)
- `DISABLE_MIGRATIONS` - отключить автоматические миграции (1 для отключения)


## Разработка

### Миграции базы данных

```bash
# Создание миграции
alembic revision --autogenerate -m "description"

# Применение миграций
alembic upgrade head

# Откат миграций
alembic downgrade -1
```

### Добавление новых тестов

📖 **Подробная информация**: [tests/README.md](tests/README.md)

## Производительность

- **Время выполнения тестов**: ~6 секунд
- **API Response Time**: <100ms
- **Database Connection Pool**: 5-20 соединений

## Разработка

### Миграции базы данных

```bash
# Создание новой миграции
alembic revision --autogenerate -m "description"

# Применение миграций
alembic upgrade head

# Откат миграции
alembic downgrade -1

# Проверка статуса миграций
alembic check
```

### Добавление новых зависимостей

```bash
# Добавление зависимости
pip install package_name

# Обновление requirements.txt
pip freeze > requirements.txt

# Для dev зависимостей используйте pyproject.toml
```

### Форматирование кода

```bash
# Автоматическое форматирование
black app tests
isort app tests

# Проверка форматирования
black --check app tests
isort --check-only app tests
```

### Линтинг

```bash
# Запуск линтеров
ruff check app tests
flake8 app tests
```

## Лицензия

MIT License
